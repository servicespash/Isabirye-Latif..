import React from 'react';
import { Link } from 'react-router-dom';

export const ManifestoPulseBanner: React.FC = () => (
  <Link to="/manifesto" className="pulse-banner">
    // READ THE ARCHITECT MANIFESTO
  </Link>
);
